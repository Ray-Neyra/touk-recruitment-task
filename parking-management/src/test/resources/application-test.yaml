spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:touk;INIT=CREATE SCHEMA IF NOT EXISTS parkingmanagement\;set schema parkingmanagement
    username: sa
  jpa:
    show-sql: true
    liquibase:
      enabled: true
      drop-first: true
      default-schema: parkingmanagement
      contexts: test
